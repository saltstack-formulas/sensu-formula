{% set files = salt['grains.filter_by']({
    'default': {
        'files': {
            'user': 'sensu',
            'group': 'sensu',
        },
    },
    'Windows': {
        'files': {
            'user': 'Administrator',
            'group': 'nowindowsgroup',
            },
    },
}, merge=salt['pillar.get']('sensu'), default='default') %}

